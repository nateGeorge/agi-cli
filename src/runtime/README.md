# Runtime (future)

Optional native accelerators and SIMD micro-kernels may ship as separate artifacts. The reference distribution is the `agi` shell entrypoint plus declarative configuration under `config/`.
