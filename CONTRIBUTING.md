# Contributing

Please open an issue before large changes. Keep commits focused; match existing shell style for `agi` and installer scripts.

- Run `make check` before pushing.
- Configuration changes should stay backward compatible for existing `agi` sessions unless versioned explicitly.
