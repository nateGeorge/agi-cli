# Weights

Model checkpoints are **not** included in this repository.

Licensed deployments receive signed bundles out-of-band; the CLI remains a thin bootstrap and visualization layer.

For integrity tooling, see `scripts/hash_weights.sh` (placeholder).

If you create a local `weights/` directory for experiments, it is ignored by git (see root `.gitignore`).
